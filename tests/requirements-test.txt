# Test Dependencies for Econome Architecture Testing
# Install with: pip install -r tests/requirements-test.txt

# Core testing framework
pytest>=8.0.0
pytest-asyncio>=0.24.0

# HTTP testing
requests>=2.31.0
httpx>=0.27.0

# FastAPI testing
fastapi[testing]>=0.115.0

# Audio processing (for tests)
numpy>=1.24.0
# pydub>=0.25.1  # Optional - for audio format testing

# System monitoring (for performance tests)
psutil>=5.9.0

# Mocking and utilities
unittest-mock>=1.0.1

# Test coverage (optional)
pytest-cov>=4.0.0

# For base64 and data processing in tests
# (included in Python standard library, no install needed)

# Note: These are testing dependencies only
# Production dependencies are in ../requirements.txt 