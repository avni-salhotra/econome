# ===================================
# ECONOME - PRODUCTION REQUIREMENTS
# ===================================
# SRE-optimized: Only packages actually used in codebase
# Fixes protobuf conflicts by letting pip resolve versions

# ===================================
# WEB FRAMEWORK & API
# ===================================
fastapi>=0.115.0
uvicorn>=0.34.0
websockets>=15.0.0
python-multipart>=0.0.20
pydantic>=2.11.0

# ===================================
# GOOGLE CLOUD SERVICES
# ===================================
# Speech-to-Text V2 API (REQUIRED)
google-cloud-speech>=2.33.0

# Gemini AI (REQUIRED)
google-generativeai>=0.8.0

# Firestore for session management (REQUIRED)
google-cloud-firestore>=2.20.0

# AI Platform for Vertex AI (REQUIRED)
google-cloud-aiplatform>=1.70.0

# BigQuery for analytics (OPTIONAL)
google-cloud-bigquery>=3.28.0

# Authentication (REQUIRED)
google-auth>=2.40.0

# ===================================
# AUDIO PROCESSING (REQUIRED)
# ===================================
sounddevice>=0.5.0
scipy>=1.14.0
numpy>=1.23.5,<2.3
pydub>=0.25.0
pydub>=0.25.0

# ===================================
# UTILITIES
# ===================================
python-dotenv>=1.1.0
requests>=2.32.0

# ===================================
# SRE NOTES
# ===================================
# - Used version ranges instead of pinned versions for flexibility
# - Let pip resolve protobuf version automatically
# - Only includes packages actually imported in src/ code
